<UserControl x:Class="teklif_programi.view.FirmaEkle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="900" d:DesignWidth="1350">

    <UserControl.Resources>
        <!-- Modern Buton Stili -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#13183E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Source Sans Pro"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4B4B4B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#ededed" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Başlık -->
            <RowDefinition Height="*"/>     <!-- Form ve Butonlar -->
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20" HorizontalAlignment="Center">
            <TextBlock Text="🏢⁺  Müşteri Firma Ekle" 
                       FontSize="32" 
                       FontWeight="Bold" 
                       FontFamily="Segoe UI"
                       Foreground="#13183E" />
            <Border Height="2" Width="360" Background="#13183E" CornerRadius="1" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Form ve Butonlar -->
        <StackPanel Grid.Row="1" Width="700" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="70,70,70,0" >
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Firma Adı -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Firma Ad" FontSize="14" Foreground="#13183E" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtFirmaAdi" Height="40" FontSize="14" BorderBrush="#ccc" BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" Background="White"/>
                </StackPanel>

                <!-- Firma Adresi -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Firma Adres" FontSize="14" Foreground="#13183E" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtAdres" Height="40" FontSize="14" BorderBrush="#ccc" BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" Background="White"/>
                </StackPanel>
            </Grid>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Firma Telefonu -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Firma Telefon" FontSize="14" Foreground="#13183E" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtTelefon" Height="40" FontSize="14" BorderBrush="#ccc" BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" Background="White"/>
                </StackPanel>

                <!-- Firma Emaili -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Firma Email" FontSize="14" Foreground="#13183E" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtEmail" Height="40" FontSize="14" BorderBrush="#ccc" BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" Background="White"/>
                </StackPanel>
            </Grid>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Firma Vergi Numarası -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Vergi Numarası" FontSize="14" Foreground="#13183E" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtVergiNumarasi" Height="40" FontSize="14" BorderBrush="#ccc" BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" Background="White"/>
                </StackPanel>

                <!-- Firma Vergi Dairesi     -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Vergi Dairesi" FontSize="14" Foreground="#13183E" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtVergiDairesi" Height="40" FontSize="14" BorderBrush="#ccc" BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" Background="White"/>
                </StackPanel>
            </Grid>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Firma İlgili Kişi -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="İlgili Kişi" FontSize="14" Foreground="#13183E" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtilgiliKisi" Height="40" FontSize="14" BorderBrush="#ccc" BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" Background="White"/>
                </StackPanel>

                <!-- Firma İlgili Kişi Telefonu -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="İlgili Kişi Telefonu" FontSize="14" Foreground="#13183E" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtilgiliKisiTelefonu" Height="40" FontSize="14" BorderBrush="#ccc" BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" Background="White"/>
                </StackPanel>
            </Grid>


            <!-- Butonlar aynı hizada ve hemen altında -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="30">
                <Button Content="Kaydet" Style="{StaticResource ModernButtonStyle}" Width="180" Margin="0,0,40,0" Click="Button_Click"/>
                <Button Content="İptal" Style="{StaticResource ModernButtonStyle}" Background="#C0392B" Width="180" Click="Button_Click_1"/>
                
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
