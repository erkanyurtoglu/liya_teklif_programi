<Window x:Class="teklif_programi.view.UrunMaliyetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ürün Maliyetleri" Height="450" Width="600"
        WindowStartupLocation="CenterScreen" Background="#ededed">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="dataGridMasraflar" Grid.Row="0" AutoGenerateColumns="False"
                  CanUserAddRows="True" CanUserDeleteRows="True" Margin="0,0,0,10"
                  RowHeight="30" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Açıklama" Width="*"
                                   Binding="{Binding Aciklama, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="Tutar" Width="150"
                                   Binding="{Binding Tutar, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:N2}}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <TextBlock Text="Toplam Maliyet:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock x:Name="txtToplamMaliyet" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Kaydet" Width="100" Margin="5" Click="BtnKaydet_Click"/>
            <Button Content="İptal" Width="100" Margin="5" Click="BtnIptal_Click" Background="#C0392B" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>