<Window x:Class="teklif_programi.view.TeklifDetayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Label Content="Firma:"/>
            <Label x:Name="lblFirma" Margin="5,0"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            <Label Content="Tarih:"/>
            <Label x:Name="lblTarih" Margin="5,0"/>
        </StackPanel>
        <DataGrid x:Name="dataGridDetaylar" Grid.Row="2" Margin="5" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ürün Kodu" Binding="{Binding Urun.UrunKoduID}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Açıklama" Binding="{Binding Urun.Aciklama}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Adet" Binding="{Binding Adet, UpdateSourceTrigger=LostFocus}" />
                <DataGridTextColumn Header="Birim Fiyat" Binding="{Binding BirimFiyat, StringFormat=C2, UpdateSourceTrigger=LostFocus}" />
                <DataGridTextColumn Header="Toplam Fiyat" Binding="{Binding ToplamFiyat, StringFormat=C2}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Label Content="Toplam Tutar:"/>
            <Label x:Name="lblToplamTutar" Margin="5,0"/>
            <Button Content="Güncelle" Click="BtnGuncelle_Click" Margin="10,0" Width="100"/>
            <Button Content="PDF İndir" Click="BtnPdfIndir_Click" Margin="10,0" Width="100"/>
            <Button Content="İptal" Click="Window_Closed" Margin="10,0" Width="100"/>
        </StackPanel>
    </Grid>
</Window>